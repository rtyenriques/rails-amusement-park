
<%= form_tag('/signin') do %>
  <%= label_tag "Users" %><br>
  
  <%= select_tag "user_name", options_from_collection_for_select(User.all, :id, :name)%>
  <%= password_field_tag "password"%>

  <%= submit_tag "Sign In" %>
<% end %>